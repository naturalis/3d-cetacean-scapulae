#!/usr/bin/Rscript
# install script for R packages. Please run this in bash, or R.
options(rgl.useNULL=TRUE) # This option is used to install the packages in a shell env like linux.
install.packages('Morpho')
install.packages("ggplot2")